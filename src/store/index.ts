import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

// const requireModules = require.context(
//   './modules',
//   true,
//   /index\w+\.(ts)$/,
// );

export default new Vuex.Store({
  modules: {

  },
});
